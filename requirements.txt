# Core API / workflow dependencies
dashscope>=1.20
python-dotenv>=1.0
requests>=2.31
pillow>=10.0

# Numeric / plotting dependencies
numpy>=2.0,<3
scipy>=1.14
matplotlib>=3.8

# Official ITTE evaluation backends
# If you need a specific CUDA build, install torch/torchvision from
# https://pytorch.org/get-started/locally/ first, then run this file.
torch>=2.4,<3
torchvision>=0.19,<1
transformers>=5.0
huggingface_hub>=0.24
modelscope>=1.20
lpips==0.1.4
DISTS-pytorch==0.1
